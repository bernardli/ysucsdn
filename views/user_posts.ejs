<%- include('header') %>

<link rel="stylesheet" href="../css/posts.css">

<%- include('components/user-info', { author: author }) %>

<div class="limit-post-content">
<%- include('components/limit-post-content', { posts: posts }) %>
</div>

<% if(posts.length == 0 && page == 1) { %>
        <div class="eight wide column">
            <div class="ui segment">
                该用户毛都没写/(ㄒoㄒ)/~~
            </div>
        </div>
<% } else if (posts.length == 5){ %>
<div class="menu-load">
    <div class="item-load center" id="loadmore_user">
        <span>----- 你觉得再看几篇吼不吼啊o(￣ε￣*) -----</span>
    </div>
</div>
<% } %>
<% else{ %>
<div class="menu-load">
    <div class="item-load center" id="no_user">
        <span>----- 人呐就是不知道 (っ*´Д`)っ 已经没有更多文章了 -----</span>
    </div>
</div>
<% } %>

<%- include('components/back-to-top') %>

<%- include('footer') %>